{
  "name": "mindfulcounter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run tests/unit",
    "test:watch": "vitest watch tests/unit",
    "test:e2e": "npx playwright test",
    "test:all": "vitest run tests/unit && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "jsdom": "^29.1.1",
    "vitest": "^3.1.0"
  }
}
